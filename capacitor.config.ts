import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'parcial-ii-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
